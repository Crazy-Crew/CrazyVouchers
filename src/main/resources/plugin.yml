name: Vouchers
main: "${group}.Vouchers"
version: ${version}
api-version: 1.13
description: ${description}

softdepend:
  - PlaceholderAPI

commands:
  vouchers:
    description: Give yourself a Voucher!
    aliases: [voucher]

permissions:
  voucher.admin:
    default: op

  voucher.redeem:
    default: op