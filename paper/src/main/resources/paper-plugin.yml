name: '${name}'
main: '${group}.${name}'

version: '${version}'
api-version: '${minecraft}'
description: '${description}'

authors:
  - ryderbelserion
  - BadBones69

folia-supported: true

dependencies:
  server:
    # Placeholder plugins
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true

    # Custom item plugins
    CraftEngine:
      load: BEFORE
      required: false
      join-classpath: true
    ItemsAdder:
      load: BEFORE
      required: false
      join-classpath: true
    Oraxen:
      load: BEFORE
      required: false
      join-classpath: true
    Nexo:
      load: BEFORE
      required: false
      join-classpath: true

    # Skull plugins
    HeadDatabase:
      load: BEFORE
      required: false
      join-classpath: true

    # Cosmetic plugins
    HMCWraps:
      load: BEFORE
      required: false
      join-classpath: true