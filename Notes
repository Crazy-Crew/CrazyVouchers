Reports/Request:
  - (Feature Request) Add a option to make vouchers only work in a spasific region.
  - (Feature Request) Add it so a command is ran by the player instead of by console.
  
Bug Fixes:
  - 